{% extends 'base.html.twig' %}

{% block title %}Order{% endblock %}

{% block body %}
    <h1>Order</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ orders.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ orders.title }}</td>
            </tr>
            <tr>
                <th>Foodid</th>
                <td>{{ orders.foodid }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ orders.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ orders.image }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ orders.price }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ orders.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_orders_index') }}">back to list</a>

    <a href="{{ path('admin_orders_edit', {'id': orders.id}) }}">edit</a>

    {{ include('admin/orders/_delete_form.html.twig') }}
{% endblock %}
